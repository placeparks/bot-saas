# Wrapper Dockerfile that adds our custom entrypoint to OpenClaw
FROM ghcr.io/openclaw/openclaw:latest

# Create minimal entrypoint - use cat with heredoc (proper syntax this time)
RUN cat > /entrypoint.sh <<'EOF' \
&& chmod +x /entrypoint.sh
#!/bin/sh
set -e
mkdir -p /tmp/.openclaw
printf '%s' "$OPENCLAW_CONFIG" > /tmp/.openclaw/config.json
exec openclaw --config /tmp/.openclaw/config.json
EOF

ENTRYPOINT ["/entrypoint.sh"]
